{
  "name": "medsearch-build",
  "private": true,
  "scripts": {
    "generate": "bun run generator/index.ts",
    "sync": "bun run scripts/sync-sam-database.ts",
    "serve": "python3 -m http.server 3000 -d dist",
    "clean": "rm -rf dist"
  },
  "type": "module"
}
