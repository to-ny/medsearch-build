{
  "name": "medsearch-build",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "bun run generator/index.ts",
    "serve": "bunx serve dist -l 3000",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pg": "^8.13.3"
  },
  "devDependencies": {
    "@types/pg": "^8.11.11"
  }
}
